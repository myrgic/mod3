"""mod3 Pipecat integration — optional; requires ``pipecat-ai`` to be installed.

Install with:
    pip install mod3[pipecat]

The integration is guarded so importing this package without pipecat installed
raises a clear ``ImportError`` rather than a confusing AttributeError.
"""
