"""mod3 integration packages — bindings for external frameworks.

Each subpackage adapts mod3's inference primitives to a target ecosystem
(Pipecat, etc.). All subpackages are optional; only their own extras need
to be installed for them to load.
"""
